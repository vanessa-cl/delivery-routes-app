import { css } from "styled-components";

export const button = css`
  background-color: var(--mid-gray);
  color: var(--white);
  border-radius: 5px;
  border: none;
  font-size: 16px;
  font-family: "Outfit";
  font-weight: 500;
  width: 156px;
  height: 38px;
  cursor: pointer;
`;
